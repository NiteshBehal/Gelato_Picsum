<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gelato.picsum">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GelatoPicsumApplication"
        android:icon="@drawable/gelato_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gelato_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.GelatoPicsum">
        <activity
            android:name=".ui.activities.ImageListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>